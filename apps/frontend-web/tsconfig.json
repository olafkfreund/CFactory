{
  "//": "Extends the vendored fleet TypeScript baseline (Factory#546). Everything the baseline sets - strict, noUncheckedIndexedAccess, exactOptionalPropertyTypes, noImplicitOverride, noImplicitReturns, verbatimModuleSyntax, target/module/lib - is inherited and MUST NOT be re-opened here. The options below are the ones the baseline does not set, or that tighten it.",
  "extends": "../../standards/tsconfig.base.json",
  "compilerOptions": {
    "useDefineForClassFields": true,
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx"
  },
  "include": ["src"]
}
